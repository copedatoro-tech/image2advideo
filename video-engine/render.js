const fs = require("fs");
const path = require("path");
const { v4: uuidv4 } = require("uuid");

async function generateVideo(imagePath) {
  // ⚠️ NU SCHIMBA LOGICA DINĂUNTRU
  // doar las-o așa cum e acum
}

module.exports = {
  generateVideo,
};
